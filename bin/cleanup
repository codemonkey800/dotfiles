#!/usr/bin/env fish

echo -e 'y\ny\n' | sudo pacman -Scc

set pkgs (pacman -Qqdt)
if test (count $pkgs) -gt 0
    sudo pacman -Rscun --noconfirm $pkgs
end

set files /tmp/{yaourt*,npm*}
echo 'Removing' (count $files) 'files from /tmp!'
sudo rm -rf $files

echo y | rmshit

